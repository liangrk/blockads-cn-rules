# Tencent GDT (优量汇) ad SDK domains.
# Format: one domain per line (plain, or "0.0.0.0 domain").
# Populate only from packet captures or verified ad-only subdomains.
# Tencent GDT (优量汇) ad request/bidding API domains.
# Verified unblocked on device 2026-09-05 (v3/c3/v2mi.gdt.qq.com resolved normally).
# gdt.qq.com suffix-match covers all subdomains; purely an ad SDK domain.
gdt.qq.com

# WeChat in-app ad serving (under weixin.qq.com umbrella -> '!' exception)
as.weixin.qq.com
